import{isNullOrWhiteSpaces}from"./general.js";export default class Filter{constructor(t,e){if(isNullOrWhiteSpaces(t,e))throw"[FILTER] Some argument is null or white spaces!";this.filterButtons=document.querySelectorAll(t),this.filterContentElements=document.querySelectorAll(e);for(const s of this.filterButtons)s.addEventListener("click",(()=>{this.filtContentByType(s,this.filterContentElements)}))}filtContentByType(t,e){let s=t.dataset.filtContent;for(const l of e)"all"==s||l.dataset.contentType.includes(s)?l.style.display="":l.style.display="none";for(const l of this.filterButtons)l==t?l.classList.add("active"):l.classList.remove("active")}}