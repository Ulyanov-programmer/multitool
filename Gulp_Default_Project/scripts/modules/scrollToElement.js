import{isNullOrWhiteSpaces}from"./general.js";export default class ScrollElement{constructor(e,l){if(isNullOrWhiteSpaces(e))throw new Error("[SCROLL-ELEMENTS] Incorrect scroll-buttons selector!");let t=document.querySelectorAll(e);for(let o of t)o.addEventListener("click",(()=>{this.scrollToElement(o)}));if(0==isNullOrWhiteSpaces(l)){let e=document.querySelector(l).clientHeight;ScrollElement.fixedHeaderHeight=e}}scrollToElement(e){let l=document.querySelector(e.dataset.scrollTo);if(null==l)throw new Error("[SCROLL-ELEMENTS] Something wrong with scrollElement!");let t=window.pageYOffset+l.getBoundingClientRect().top;window.scrollTo({top:t-ScrollElement.fixedHeaderHeight,behavior:"smooth"})}}ScrollElement.fixedHeaderHeight=0;