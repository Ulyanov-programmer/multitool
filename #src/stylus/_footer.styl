@import "_mixins"

.footer
  background: rgb(99, 99, 99)

  flex-set row nowrap
  justify-content: center
  min-height: 100px
  // background: url('../img/') no-repeat center / cover

  &__body
    a
      color: #fff
