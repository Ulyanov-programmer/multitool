.spoiler {
  position: relative;
  transition: all 0.3s ease;

  &-btn {
    display: flex;
    flex-flow: row nowrap;
    width: 100%;
    cursor: pointer;
  }

  /* &-content {
    &.active {}
  } */
}
