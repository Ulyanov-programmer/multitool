.submenu-wrapper {
  position: relative;
}
.submenu-menu {
  position: absolute;
  top: 100%;
  z-index: 1;
  @mixin hide;
  transition: all 0.2s ease;
  background: white;
  width: max-content;

  &.show {
    @mixin show;
  }

  &_to-left {
    right: 0;
  }
  &_to-right {
    left: 0;
  }
  &_center {
    left: 50%;
    transform: translate(-50%, 0);
  }
}
