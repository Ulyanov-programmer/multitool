:root {
  --sidebarHideWidth: 1440px;
  --hiddenSidebarWidth: 70vw;
}

.sidebar {
  @media (max-width: $sidebarHideWidth) {
    z-index: 3;
    position: fixed;
    top: 0;

    height: 100vh;
    width: var(--hiddenSidebarWidth);

    background: gray;
    transition: all 0.5s ease;
  }

  /* ? from left to right */
  right: 100vw;
  &.active {
    right: calc(100vw - var(--hiddenSidebarWidth));
  }

  /* ? from right to left */
  /* left: 100vw;
  &.active {
    left: calc(100vw - var(--hiddenSidebarWidth));
  } */
}
[data-toggle-sidebar-id] {
  @media (min-width: $sidebarHideWidth) {
    display: none;
  }
  @media (max-width: $sidebarHideWidth) {
    z-index: 2;
    position: fixed;
    top: 0;
  }

  /* ? from left to right */
  left: 100%;

  /* ? from right to left */
  /* right: 100%; */
}

.swipe_window__swipe_area {
  z-index: 1;
  position: fixed;
  top: 0;
  left: 0;

  height: 100vh;
  width: 20vw;

  @media (min-width: $sidebarHideWidth) {
    display: none;
  }
  &.active {
    width: 100vw;
  }
}
